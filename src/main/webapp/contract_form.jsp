<%@ page contentType="text/html;charset=UTF-8" pageEncoding="UTF-8" %><%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %><!DOCTYPE html><html lang="zh-CN"><head><jsp:include page="header.jsp"/></head><body class="hold-transition sidebar-mini"><div class="wrapper"><jsp:include page="navbar.jsp"/><jsp:include page="sidebar.jsp"/><div class="content-wrapper"><div class="content-header"><div class="container-fluid"><h1>${empty contract.id?'新增合同':'编辑合同'}</h1></div></div><section class="content"><div class="container-fluid"><form method="post" action="${pageContext.request.contextPath}/contract/save"><input type="hidden" name="id" value="${contract.id}"><div class="card"><div class="card-header"><h3 class="card-title">合同基本信息</h3></div><div class="card-body"><div class="row"><div class="form-group col-md-6"><label>合同名称 *</label><input name="contractName" value="${contract.contractName}" class="form-control" required></div><div class="form-group col-md-3"><label>关联客户 *</label><select name="customerId" class="form-control" required><option value="">请选择</option><c:forEach items="${customerList}" var="c"><option value="${c.id}" ${contract.customerId==c.id?'selected':''}>${c.customerName}</option></c:forEach></select></div><div class="form-group col-md-3"><label>业务状态</label><select name="businessStatus" class="form-control"><c:forEach items="${['执行中','已到期','已终止']}" var="v"><option ${contract.businessStatus==v?'selected':''}>${v}</option></c:forEach></select></div><input type="hidden" name="opportunityId" value="${contract.opportunityId}"><c:if test="${not empty contract.opportunityTitle}"><div class="form-group col-12"><div class="alert alert-info mb-0"><i class="fas fa-link mr-2"></i>来自成交商机：<c:out value="${contract.opportunityTitle}"/></div></div></c:if><div class="form-group col-md-4"><label>签订日期</label><input type="date" name="signedDate" value="${contract.signedDate}" class="form-control"></div><div class="form-group col-md-4"><label>开始日期</label><input type="date" name="startDate" value="${contract.startDate}" class="form-control"></div><div class="form-group col-md-4"><label>到期日期</label><input type="date" name="endDate" value="${contract.endDate}" class="form-control"></div><div class="form-group col-md-6"><label>付款条款</label><textarea name="paymentTerms" class="form-control" rows="3"><c:out value="${contract.paymentTerms}"/></textarea></div><div class="form-group col-md-6"><label>备注</label><textarea name="remarks" class="form-control" rows="3"><c:out value="${contract.remarks}"/></textarea></div></div></div></div><div class="card"><div class="card-header"><h3 class="card-title">合同产品明细</h3><button type="button" id="addRow" class="btn btn-sm btn-outline-primary float-right"><i class="fas fa-plus"></i> 添加行</button></div><div class="card-body p-0"><table class="table line-items mb-0"><thead><tr><th>产品</th><th>数量</th><th>单价</th><th>小计</th><th></th></tr></thead><tbody id="items"><c:forEach items="${contract.items}" var="item"><tr><td><select name="items[][productId]" class="form-control product" required><option value="">请选择</option><c:forEach items="${productList}" var="p"><option value="${p.id}" data-price="${p.unitPrice}" ${item.productId==p.id?'selected':''}>${p.productName}</option></c:forEach></select></td><td><input name="items[][quantity]" class="form-control qty" type="number" min="1" value="${item.quantity}" required></td><td><input name="items[][unitPrice]" class="form-control price" type="number" min="0" step="0.01" value="${item.unitPrice}" required></td><td class="money subtotal">¥${item.subtotal}</td><td><button type="button" class="btn btn-sm btn-danger remove"><i class="fas fa-times"></i></button></td></tr></c:forEach></tbody><tfoot><tr><td colspan="3" class="text-right"><strong>合同金额</strong></td><td class="money text-primary" id="total">¥0.00</td><td></td></tr></tfoot></table></div><div class="card-footer text-right"><a href="${pageContext.request.contextPath}/contract/list" class="btn btn-light">取消</a> <button class="btn btn-primary"><i class="fas fa-save"></i> 保存合同</button></div></div></form></div></section></div><jsp:include page="footer.jsp"/></div><jsp:include page="scripts.jsp"/><template id="rowTemplate"><tr><td><select name="items[][productId]" class="form-control product" required><option value="">请选择</option><c:forEach items="${productList}" var="p"><option value="${p.id}" data-price="${p.unitPrice}">${p.productName}</option></c:forEach></select></td><td><input name="items[][quantity]" class="form-control qty" type="number" min="1" value="1" required></td><td><input name="items[][unitPrice]" class="form-control price" type="number" min="0" step="0.01" value="0" required></td><td class="money subtotal">¥0.00</td><td><button type="button" class="btn btn-sm btn-danger remove"><i class="fas fa-times"></i></button></td></tr></template><script>const items=document.getElementById('items');function add(){items.append(document.getElementById('rowTemplate').content.cloneNode(true))}function calc(){let t=0;items.querySelectorAll('tr').forEach(r=>{const s=(parseFloat(r.querySelector('.qty').value)||0)*(parseFloat(r.querySelector('.price').value)||0);r.querySelector('.subtotal').textContent='¥'+s.toFixed(2);t+=s});document.getElementById('total').textContent='¥'+t.toFixed(2)}items.addEventListener('change',e=>{if(e.target.classList.contains('product'))e.target.closest('tr').querySelector('.price').value=e.target.selectedOptions[0].dataset.price||0;calc()});items.addEventListener('input',calc);items.addEventListener('click',e=>{const b=e.target.closest('.remove');if(b){b.closest('tr').remove();if(!items.children.length)add();calc()}});document.getElementById('addRow').onclick=()=>{add();calc()};if(!items.children.length)add();calc();</script></body></html>
